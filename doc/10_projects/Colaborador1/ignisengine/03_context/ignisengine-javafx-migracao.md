---
tags: [ignisengine, javafx, migracao, ui, decision, architecture]
updated: 2026-06-17
revisao: F4 + Configuracoes + Inspector + undo/redo + gizmos/colliders no FX + settings do code editor; crash nativo 0xC0000409 ADIADO — 2026-06-17
---

## Definição

Migração incremental da interface do editor IgnisEngine de Swing/AWT para JavaFX 17, mantendo o núcleo de render da engine.

## Contexto

Editor hoje é Swing/AWT: 38 arquivos usam swing/awt, monolito `editor/Editor.java` (~5962 linhas), núcleo `core/Game.java extends java.awt.Canvas` (BufferStrategy + Graphics2D). Plano completo no projeto: `IgnisEngine/doc/JAVAFX_MIGRATION_PLAN.md`.

## Decisões-Chave

- Branches (2026-06-14): `Legado` = versao estavel Swing + marketplace (preservada, nao migra); `main` = desenvolvimento da migracao JavaFX. Cada fase mergeavel; Legado e o ponto de retorno seguro.
- Incremental, não big-bang: Swing e JavaFX convivem durante a transição.
- Interop: `SwingNode` (embutir Swing na cena FX), `JFXPanel` (FX dentro de Swing), `SwingFXUtils` (BufferedImage ↔ WritableImage).
- Ponte de render do Viewport: render offscreen in BufferedImage (mesmo Graphics2D) → `SwingFXUtils.toFXImage` → `javafx.scene.canvas.Canvas` via AnimationTimer. Desacopla loop do jogo da janela; remove BufferStrategy no editor.
- Rejeitado: embutir Canvas AWT pesado direto na cena FX (heavyweight/lightweight quebra foco/render).
- Reescrever pipeline gráfico NÃO faz parte da migração.

## Fases

- F0 (FEITO, so na main): deps JavaFX 17 no pom.xml + javafx-maven-plugin + pacote `com.ignis.editor.fx`. Compila.
- F1 (FEITO, so na main): IgnisEditorApp (BorderPane/MenuBar/SplitPane) + ponte de render `Game.renderWorldTo` -> BufferedImage -> SwingFXUtils -> Canvas (AnimationTimer); Hierarchy nativa (TreeView); Inspector placeholder; cena de amostra. Rodar: `mvnw javafx:run`.
- F2 (FEITO, so na main): abrir projeto .ignis real (FileChooser -> IgnisProjectIO.load) no viewport; selecao Hierarchy<->viewport (contorno via Game.renderWorldTo com selected); Inspector GridPane editavel (nome/x/y/w/h/rot/visivel) escreve no GameObject ao vivo. Pendente: ToolBar, atalhos, Play/Stop.
- F3 (CONCLUIDA na main): dividida Claude/Gemini. Claude FEITO: BuildDialog nativo (FxBuildDialog), ToolBar (Abrir/Build/Play/Stop), atalhos (Ctrl+O/Ctrl+B/F5/F6), Play/Stop ligados ao loop (playWorld/start, stopWorld/stop, ScriptManager ao abrir projeto). Gemini FEITO: FxCommunityWindow, FxNotesWindow, FxAnimationEditor, FxImageEditor/FxPaintCanvas, FxAudioEditor, FxCodeEditor.
- Fiacao do menu COMPLETA (2026-06-15, commit 63ad0ee): o menu Ferramentas da casca IgnisEditorApp abre SO janelas JavaFX (Audio/Imagens/Animacao/Notas/Comunidade/Editor de Codigo/Build) — sem fallback Swing. FxCodeEditor foi o ultimo a ser ligado: item -> ChoiceDialog com scripts do projeto (ScriptManager.listAvailableScripts) + "Novo script" (createNewScript) -> new FxCodeEditor(null, scriptManager, scriptName) (param Editor Swing = null, ja tratado por null-check).
- Input no viewport FX FEITO (2026-06-15, commit 63ad0ee): teclado/mouse do Canvas FX roteados ao singleton com.ignis.core.Input fabricando java.awt.event.KeyEvent/MouseEvent e chamando os callbacks AWT publicos do Input (IgnisEditorApp.wireFxInputToEngine). Puramente ADITIVO. Teclado so em Play; mouse sempre; Canvas focavel.
- F3.5 — Casca do editor (ciclo de vida de projeto + mecanicas) FEITO (2026-06-15, commit 9f0be7c): tudo ADITIVO (nada em com.ignis.core).
  - Tela de selecao de projeto ao abrir (FxProjectStartupDialog) com LISTA visual de projetos + recentes (★) — melhora o startup Swing (que so tinha 3 botoes New/Open/Exit).
  - Persistencia estilo VSCode (EditorPrefs) em user.home/.ignis/editor-prefs.json: ultimo projeto + recentes (MRU, dedupe, clearMissing). Submenu "Abrir recente".
  - Menu Arquivo completo: Novo/Abrir/Abrir-recente/Selecionar/Salvar(Ctrl+S)/Salvar-como(Ctrl+Shift+S)/Fechar/Trocar. currentProject mantido; o .ignis AGORA e gravado (antes o FX nunca chamava IgnisProjectIO.save). syncEntitiesToScene espelha doSaveProject.
  - Mecanicas: menu "Cena" + ContextMenu na Hierarchy + teclas (Delete/F2/Ctrl+D) — criar (EntityFactory), duplicar, deletar, renomear, reordenar z-order. Asset Browser: TreeView<File> do projeto (abre no duplo-clique).
- Trabalho Gemini/usuario (2026-06-15): ferramentas de viewport (Mover/Rotacionar/Redimensionar/Zoom/Reset Cam/Focus Selected), Correcao de Bugs e Auditoria de Mouse (resolvido o loop de mouse warping do Robot em Game.java via isShowing(); separacao dos botoes: esquerdo=selecao, direito=menus de contexto em Hierarchy/Assets/subeditores), integracao do FxCodeEditor interno na arvore de Assets, ajuste visual da numeracao de linhas via CSS !important.
- Correcoes de selecao/inspector/picking/autosave FEITO (2026-06-15, commit 4715281, push em URSoftware/IgnisEngine):
  - Inspector agora atualiza ao clicar no viewport (selectEntity virou fonte unica e sempre chama setSelected; antes setSelected so era chamado pelo listener da arvore, que abortava por suppressSelectionEvents).
  - Selecao dupla removida (core e a fonte no botao esquerdo via handleMousePress; removido setOnMouseClicked). Clique direito nao mexe na selecao/drag do engine (SECONDARY nao vai a dispatchEvent; so menu de contexto).
  - Picking: getObjectAt(x,y,afterCurrent) CICLA entre objetos sobrepostos e respeita rotacao (hit-test rotacionado). Indicador visual: borda tracejada + alcas de canto no renderWorldTo (espelha renderSelection do Swing).
  - Regressao corrigida (revisao adversarial): clicar-e-arrastar objeto nao-selecionado num gesto unico nao cancela mais o drag (setSelected so cancela quando a selecao nao veio do engine).
  - FxCodeEditor: numeros de linha SEMPRE pretos sobre gutter claro (CSS !important + factory inline) — legiveis em qualquer tema.
  - Auto Save: item no menu Arquivo (EditorPrefs); autosave de scripts religado; autosave de projeto com dirty-flag, fora do Play, salvamento silencioso (sem Alert modal). Build mvnw compile = SUCCESS (86 fontes). PENDENTE: validacao manual em GUI.
- Rodada de bugs/perf (2026-06-16, commits 39b5367 Gemini + e9a9fd5 Claude): loop de selecao infinita (guarda game.getSelectedObject()==go + handleMouseRelease nao re-notifica); arrastes presos de gizmo (viewportMenu.setOnHidden->cancelDrag); ordenacao da Cena invertida + moveEntityUp no-op (remove newIndex-- em Game.moveEntityToIndex, inverte topo/fundo nos menus); clique direito agora seleciona o item sob o cursor na Hierarchy E no Asset Browser (cellFactory no SECONDARY); travadas reduzidas com suppressAwtRepaint (Game ignora repaint() do AWT sob o editor FX). Detalhes em ignisengine-auditoria.md e doc/CORRECOES_BUGS_FX_2026-06-16.md.
- Roadmap pos-F4 (paridade pendente): undo/redo (Command), Inspector completo (Cor/Sprite/Collider/Camera/Scripts), anexar script a objeto, import imagem->sprite, prefabs (PrefabManager); backlog: multi-selecao, merge, drag-drop, console de erros, multi-cena, parentesco, multi-aba.
- F4-A + F4-B FEITO (2026-06-17): tema CSS escuro unificado + persistencia de layout. Aditivo; build mvnw compile = SUCCESS (87 fontes) e javafx:run sobe sem erro de parse de CSS.
  - Novo `src/com/ignis/editor/fx/ignis-dark.css` (empacotado no classpath via resources do pom) com paleta painel #2d2d2d / acento #2e8b57 / texto claro; estiliza root/menubar/toolbar/botoes/treeview/splitpane/scrollbar/contextmenu + classes utilitarias (.ignis-panel, .panel-title, .status-bar, .toolbar-label, .field-label).
  - Novo helper `FxTheme.apply(Scene)` (resolve o CSS por getResource com fallback para src/; idempotente). Casca IgnisEditorApp agora com ZERO setStyle inline (9 -> 0, via styleClass). Tema base tambem aplicado a FxCommunityWindow, FxAnimationEditor, FxProjectStartupDialog, FxBuildDialog, FxImageEditor, FxAudioEditor (inline > stylesheet, entao nao quebra o visual custom; so tema os controles default). FxCodeEditor NAO recebe (tem sistema de tema dinamico proprio).
  - F4-B: EditorPrefs ganhou bloco "layout" (getWindowBounds/isWindowMaximized/saveWindowState + getDividers/saveDividers por nome). IgnisEditorApp restaura tamanho/posicao/maximizacao (guarda isOnScreen contra monitor removido) e divisores dos SplitPane (apos 1o layout via runLater); salva em todos os pontos de saida (X da janela, menu Sair, cancelar startup).
- Janela de Configuracoes centralizada FEITA (2026-06-17, aditivo): nova FxSettingsWindow navegavel por topicos — painel esq ~1/3 rolavel (ToggleButtons .nav-item) + painel dir ~2/3 (SplitPane 0.33). Centraliza prefs antes espalhadas: Geral (Auto Save + intervalo, saiu do menu Arquivo; lembrar layout), Aparencia (tema da UI Escuro/Padrao, aplicado ao vivo via FxTheme.refreshAllWindows com marcacao de cena), Editor de Scripts (tema de sintaxe + tamanho da fonte, fonte agora lida do EditorPrefs no CSS dinamico do FxCodeEditor), Viewport (grade/tamanho/colliders no estado vivo do Game + persistido como default no startup). Menu Arquivo: "Auto Save" -> "Configuracoes…" (Ctrl+,). EditorPrefs ganhou editorTheme/rememberLayout/codeEditorFontSize/grid*. Cor de foco do CSS trocada por literal (silencia warning benigno String->Paint do modena em ComboBox). Build SUCCESS; smoke-test de javafx:run abrindo a janela sem excecao/warning.
- F4-C FEITO (2026-06-17) -> F4 COMPLETA: removida a dependencia javafx-swing. Nova FxImageBridge.toFXImage(BufferedImage,WritableImage) substitui SwingFXUtils via PixelWriter/PixelFormat (javafx-graphics): caminho rapido sem copia para TYPE_INT_ARGB contiguo (buffer do viewport por frame), fallback getRGB para sprites arbitrarios. Trocadas as 3 usagens (IgnisEditorApp ponte de render, FxAnimationEditor sprite, FxPaintCanvas composite); dependency javafx-swing removida do pom; comentario de Game.renderWorldTo atualizado. Build clean = SUCCESS; javafx:run renderiza o viewport sem javafx-swing (so warnings benignos de native-access). O "Renderer desacoplado do toolkit" se materializou como esta ponte: o engine ja renderizava em Graphics2D generico (sem BufferStrategy/Canvas no editor FX), e o unico acoplamento JavaFX restante (SwingFXUtils) foi eliminado.
- Correcoes pos-tema (2026-06-17): (1) fundo branco atras dos paineis no tema escuro = viewport branco padrao do modena nos ScrollPane; corrigido estilizando .scroll-pane e .scroll-pane>.viewport como transparente. (2) corpo do script todo verde no FxCodeEditor = sob Pattern.DOTALL o comentario de linha //.* engolia do primeiro // ate o fim do arquivo; corrigido para //[^\n]* (bloco /* */ mantem DOTALL). Provado com teste: //.* marcava 109/218 chars; //[^\n]* marca 44.
- Inspector completo FEITO (2026-06-17, paridade, aditivo): secoes por tipo recriadas a cada selecao (rebuildInspectorExtras) num ScrollPane, alem do Transform existente (sync por frame intacto). Aparencia: ColorPicker via reflexao getColor/setColor (shapes) + escolher/limpar sprite (FileChooser, caminho relativo via AssetResolver.relativize senao absoluto). Collider: ColliderType + CollisionMode + habilitado. Camera (so Camera): zoom + camera ativa. Scripts: lista de anexados + anexar (ChoiceDialog dos scripts do projeto; espelha Editor.java Swing: getScriptNames().add + ScriptManager.createScriptInstance) + remover + abrir no FxCodeEditor. Handlers ligados apos valor inicial p/ nao disparar escrita no setup. Build SUCCESS; smoke-test com objeto selecionado sem excecao.
- Tematizacao dos subeditores FEITA (2026-06-17): tema agora alterna por styleClass — stylesheet SEMPRE aplicado (paleta escura em .root, clara em .root.ignis-light) e FxTheme so liga/desliga a classe "ignis-light" na raiz; variaveis -ignis-* sempre resolvem (sem warning) e todo setStyle inline responde ao tema. Adicionadas cores semanticas (-ignis-primary/secondary/danger/info) + classes .btn-*. Convertidas as cores hardcoded e aplicado FxTheme a TODAS as cenas (inclusive dialogos internos antes sem tema) em: FxProjectStartupDialog, FxCommunityWindow (+3 dialogos), FxImageEditor (+2), FxAudioEditor (+1), FxAnimationEditor, FxNotesWindow (com.ignis.notes; passou a importar FxTheme). Mantidas as cores semanticas dos botoes DAW/AI e o tema proprio do FxCodeEditor. Bonus: o tema CLARO agora funciona de fato (toggle em Configuracoes > Aparencia). Build SUCCESS; smoke-test escuro E claro sem excecao/warning.
- Undo/Redo FEITO (2026-06-17, aditivo): novo UndoManager (pilhas undo/redo, limite 100, callback de UI). Menu "Editar" com Desfazer (Ctrl+Z) / Refazer (Ctrl+Y e Ctrl+Shift+Z), rotulados com a acao e habilitados conforme as pilhas. Cobre criar/deletar(restaura z-order)/duplicar/colar/renomear/reordenar e transformacoes por gizmo (capturadas via Game.TransformListener: estado antes em onTransformStart, comando antes->depois em onTransformEnd). Cada mutacao registra undo+redo que operam no Game vivo e atualizam Hierarchy/Inspector; historico limpo ao abrir/criar/fechar projeto. NAO cobre edicao continua de campos do Inspector por digitacao (limitacao conhecida; gizmo cobre o movimento visual). Build SUCCESS; smoke-test duplicar/undo/redo/reordenar sem excecao.
- Ajustes finos (2026-06-17): (1) contraste — estilizados texto/popup de ComboBox/ChoiceBox, ListView e toolbar do HTMLEditor (antes texto escuro sobre fundo escuro: combo de Tema em Config e controles do editor de notas). (2) Notas: a area de edicao (WebView do HTMLEditor) segue o tema via user stylesheet (Base64 data URI, setUserStyleSheetLocation; re-aplica via listener na styleClass da raiz ao trocar tema), sem alterar o HTML salvo — antes era sempre branca. (3) FxCodeEditor: ENTER mantem a identacao da linha e adiciona um nivel apos '{'; entre '{' e '}' cria a linha do meio identada com '}' alinhado (handleAutoIndentNewline no handleKeyPressedFilter). (4) Play: o eventFilter de teclas do editor (atalhos de ferramenta W/E/R/F/etc.) sai cedo quando playing — libera as teclas para os scripts do jogo (W de movimento nao chegava por causa da bind da ferramenta Mover). Build SUCCESS; smoke-test abrindo notas sem excecao.
- Ajustes 2 (2026-06-17): Inspector agora ocupa a largura disponivel (ColumnConstraints com Hgrow na coluna de campos + maxWidth nos controles; antes grudava a esquerda ao alargar). ColorPicker da secao Aparencia tematizado (fundo/texto/seta/.color-palette/.custom-color-dialog). Import imagem->sprite FEITO: ao escolher sprite fora do projeto, copia para assets/sprites (nome unico) e salva caminho relativo (portavel; antes salvava caminho absoluto externo). [backlog import-sprite: FEITO]
- CRASH NATIVO (ADIADO por decisao do usuario em 2026-06-17 — "nao faremos isso por hora"): `mvnw javafx:run` saindo com -1073740791 = 0xC0000409 (STATUS_STACK_BUFFER_OVERRUN, crash NATIVO, nao Java) + relato "editor de scripts nao abre". NAO reproduzido nesta maquina: smokes de abrir FxCodeEditor (com Player.java), abrir Notas (WebView)+sair limpo, e carregar Game 01 + Play + sair limpo TODOS terminam BUILD SUCCESS sem excecao. Hipotese principal: coexistencia AWT (Game extends java.awt.Canvas, BufferedImage/Graphics2D/Robot/Desktop) + JavaFX no mesmo JVM no Windows — fonte classica de 0xC0000409; a extracao do Renderer (remover AWT) deve eliminar de raiz. Para diagnosticar quando retomar: pedir o hs_err_pid*.log (gravado na raiz do projeto ou %TEMP% no crash) + a acao exata que dispara. Provavel correlacao: o crash pode derrubar o app e dar a impressao de "editor nao abre" (que abre normal nos testes).
- Gizmos + colliders no viewport FX FEITO (2026-06-17): renderWorldTo (ponte FX) NAO chamava renderGizmo nem collisionManager.debugRender — por isso gizmos/colliders eram invisiveis no editor FX embora o hit-testing (arrastar) funcionasse. Agora renderWorldTo desenha, no espaco do mundo: (1) debug de colliders quando showColliders ligado; (2) os gizmos da ferramenta atual para selectedObject. O feedback visual de hover/drag (cor por eixo) ja existia em renderGizmo via hoveredGizmoMode/currentDragMode (atualizados pelos eventos de mouse roteados ao engine) — passa a aparecer agora que renderiza. Tudo no Game.renderWorldTo (core), mantendo render() do Swing intacto.
- Settings do editor de scripts centralizados FEITO (2026-06-17): Auto-Complete, tema de sintaxe e Importar/Exportar tema saem da barra do FxCodeEditor (declutter) para Configuracoes > Editor de Scripts. EditorPrefs ganhou codeAutoComplete (default true) e codeEditorCustomTheme (JSON do tema importado). FxCodeEditor: barra so com Save/Compile/Save&Compile; le auto-complete do EditorPrefs (checkAutocomplete); aplica tema salvo ao abrir via resolveSavedTheme() (suporta nome "Custom" -> importThemeFromJson do pref). Helpers estaticos themeByName/resolveSavedTheme; importThemeFromJson/exportThemeToJson ja eram static. Settings importa (valida + persiste como "Custom") e exporta (tema nomeado ou custom).
- Implementações e Pendências Finais (2026-06-30):
  - **Gerenciamento de Prefabs**: Criação e instanciação de prefabs (JSON) integrados com a UI JavaFX e o `UndoManager`. (Pendente: Prefab Mode, Vinculação ativa/herança de prefabs, diferenciação visual na arvore).
  - **Console de Logs/Erros Integrado**: Nova aba `FxConsolePanel` redirecionando `System.out`/`System.err` com níveis de cores e severidade, auto-scroll e filtros. (Pendente: Navegação duplo-clique para ir à linha do script, pesquisa e exportação).
  - **Desfazer no Inspector (Campos)**: Transações discretas ao perder foco ou pressionar Enter nos campos numéricos/nome para evitar sobrecarregar o histórico de Undo. (Pendente: sliders/drag-to-modify, suporte a colisores/scripts).
  - **Multi-seleção Completa**: Habilitado `SelectionMode.MULTIPLE` na TreeView e sincronizado com viewport por Ctrl/Shift+Click, adaptadas operações em bloco (`deleteSelected`, `duplicateSelected`) e movimentação coletiva (deltas proporcionais por `TransformListener`) sob uma única transação de Undo.
- Backlog restante (paridade/polimento final):
  - Validação manual fina em GUI.
  - Implementar Prefab Mode dedicado.
  - Navegação do erro do console (duplo clique) para abrir o FxCodeEditor na linha correspondente.
  - Undo para componentes complexos adicionais no Inspector.
  - Suporte a multi-seleção de outros tipos de ações e arrastar/soltar (drag-drop) na hierarquia.

## Riscos

- Threading FX Application Thread vs EDT: Platform.runLater / SwingUtilities.invokeLater nas fronteiras.
- Custo da cópia de imagem por frame: reutilizar buffers, PixelBuffer se preciso.
- Empacotamento (JavaFX fora da JDK): jlink/jpackage ou shade; validar no Builder.
- Input do jogo via listeners AWT: RESOLVIDO (2026-06-15) de forma aditiva — eventos do Canvas FX traduzidos para java.awt.event.KeyEvent/MouseEvent e enviados aos callbacks publicos do singleton Input, sem alterar o core (ver Fases). Resta validar em GUI.

## Links

- [[10_projects/Colaborador1/ignisengine/00_spec/ignisengine-roadmap]]
- [[10_projects/Colaborador1/ignisengine/03_context/ignisengine-java-17]]
- [[10_projects/Colaborador1/ignisengine/03_context/ignisengine-marketplace]]
